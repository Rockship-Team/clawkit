{
  "version": "1.0.0",
  "requires_bins": ["vault-cli"],
  "setup_prompts": [
    {
      "key": "vault_path",
      "prompt": "Duong dan den Obsidian vault (vi du: ~/Documents/MyVault):",
      "required": true
    }
  ],
  "exclude": ["cmd", "*.go"]
}
